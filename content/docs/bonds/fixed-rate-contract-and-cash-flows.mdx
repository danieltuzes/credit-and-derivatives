---
title: Fixed-rate bond contract and cash flows
description: Translate face value, coupon rate, frequency, and maturity into promised payments.
editorialStatus: draft
teaches:
  - bonds.fixed-rate-contract.interpret
  - bonds.coupon-amount.calculate
  - bonds.fixed-cashflows.identify
assumptions:
  - Level fixed coupons on a regular schedule
  - Payment frequency times maturity in years is a positive integer, so no stub period occurs
  - Principal redeemed at par on the final coupon date
  - Settlement and valuation occur on a coupon date
  - Bondholder perspective with promised payments treated as positive receipts
  - No default, accrued interest, tax, callability, or other embedded option
---

import CompactExample from '../../../src/components/examples/CompactExample.astro';
import CompactExamples from '../../../src/components/examples/CompactExamples.astro';
import AssessmentSet from '../../../src/components/assessments/AssessmentSet.astro';

## What you will be able to do

After this lesson, you should be able to:

- identify the terms that determine a toy fixed-rate bond's payments;
- calculate the level coupon amount;
- construct the complete promised cash-flow schedule.

## What a bond is

A bond is a debt claim. The issuer is the borrower; the bondholder is the
lender who owns the claim. In the fixed-rate example used here, the issuer
promises a sequence of coupon payments and repayment of principal at maturity.
The price paid to acquire the bond is a separate quantity from those promised
cash flows.

“Promises” matters. A contract can specify an amount without guaranteeing that
the issuer will pay it. This first bond lesson deliberately builds the
contractual schedule before introducing default probability or recovery later
in the credit-risk lessons. Tuckman and Serrat define a coupon bond through its
coupon rate, maturity, and face (par or principal) amount and show coupons plus
principal on the cash-flow timeline[@tuckman-serrat-fixed-income; Ch. 1 §1.1, printed p. 50 and Table 1.1].

## Read the contract before valuing it

Write the simplified contract's [[face-value]] as $F$, its
[[annual-coupon-rate]] as $c$, its [[bond-payment-frequency]] as
$m_{\mathrm B}$, and its [[maturity-time]] as $T$. Write the
[[number-of-bond-payments]] as $n$ and index those dates with the
[[bond-payment-index]] $k$. The matching [[payment-time]] is $t_k$,
measured from [[valuation-time]] $0$.

Write the level [[coupon-payment]] as $C$ and the promised
[[bond-cash-flow]] on date $k$ as $CF_k^{\mathrm{bond}}$.

The local number of dates is

$$
n=m_{\mathrm B}T.
$$

The page uses subscripted bond-frequency notation rather than reusing the
earlier [[compounding-frequency]] symbol, so the two concepts remain
distinct.

## Calculate the coupon

The level coupon payment is

$$
C=\frac{cF}{m_{\mathrm B}}.
$$

The coupon rate is applied to face value, not the bond's eventual market price.

## Construct every promised payment

For the bondholder, the promised bond cash flow is

$$
CF_k^{\mathrm{bond}}
=C+\mathbf{1}_{\{k=n\}}F.
$$

The indicator contributes face value only on the final date.

For the regular schedule, each date is measured from valuation time:

$$
t_k=\frac{k}{m_{\mathrm B}},
\qquad 0\text{ is the valuation time}.
$$

<CompactExamples
  summary="Worked examples"
  hint="Open the set, then choose a payment frequency."
  tabsLabel="Fixed-rate bond cash-flow examples"
>
  <CompactExample label="Annual coupons">

A two-year bond has USD 1,000 face value, a 5% annual coupon rate, and one
payment per year:

$$
C=\frac{0.05\times1000}{1}=\text{USD }50.
$$

| Payment index $k$ | Payment time $t_k$, years | Coupon, USD | Principal, USD | Total, USD |
| ----------------: | ------------------------: | ----------: | -------------: | ---------: |
|                 1 |                       1.0 |          50 |              0 |         50 |
|                 2 |                       2.0 |          50 |          1,000 |      1,050 |

  </CompactExample>

  <CompactExample label="Semiannual coupons">

An 18-month bond has USD 1,000 face value, a 4% annual coupon rate, and two
payments per year:

$$
C=\frac{0.04\times1000}{2}=\text{USD }20,
\qquad
n=2\times1.5=3.
$$

Its payments at 0.5, 1.0, and 1.5 years are USD 20, USD 20, and USD 1,020.

  </CompactExample>

  <CompactExample label="Quarterly coupon amount">

For USD 500 face value, a 4.8% annual coupon rate, and quarterly payments,

$$
C=\frac{0.048\times500}{4}=\text{USD }6.
$$

Maturity determines how many such payments occur; it does not change the
periodic coupon amount under the level-coupon assumption.

  </CompactExample>
</CompactExamples>

## Check your understanding

These items cover term interpretation, coupon arithmetic, and schedule
construction. Each question stays collapsed until you open it; answers and
explanations appear once you check.

<AssessmentSet />

## Model boundary and review note

This page describes promised cash flows only. It does not assign a price,
yield, payment probability, or expected recovery.

The contract terms and the level-coupon schedule follow Tuckman &
Serrat[@tuckman-serrat-fixed-income; §1.1, "Government Coupon Bonds": a bond is set by coupon rate, maturity date, and face (par, principal); each semiannual coupon is half the annual rate times face, with principal repaid at maturity (Table 1.1)]. The lesson stays `draft` pending human confirmation of
the printed locators.
