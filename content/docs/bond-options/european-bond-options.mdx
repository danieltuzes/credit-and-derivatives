---
title: European bond options
description: Define a standalone option on a bond, put its prices on one cash-price basis, and value expiry payoffs on an input lattice.
editorialStatus: draft
teaches:
  - bond-options.contract.interpret
  - bond-options.european-value.calculate
assumptions:
  - Standalone European call or put on one named bond, exercisable at one stated time
  - Exercise occurs immediately after any scheduled coupon at option expiry, so the input expiry bond values are ex-coupon
  - Bond values and strike are dirty cash prices in the same currency per bond
  - Default-free input pricing lattice with no early exercise, counterparty default, funding spread, volatility calibration, or embedded issuer option
notation:
  local:
    - key: bond-option-expiry-value
      latex: 'B_{T_{\mathrm{opt}}}^{\mathrm{dirty}}'
      meaning: Ex-coupon dirty cash value of the named bond at the European exercise instant.
      units: stated currency per bond at option expiry
      sources:
        - id: hull-options-futures
          locator: 'Ch. 28 §28.1, printed pp. 648-650, Eqs. 28.1-28.3 (European bond options on cash or dirty bond value with a cash strike).'
      seeAlso:
        - dirty-bond-price
      alignment:
        kind: competency
        introducedByCompetency: bond-options.european-value.calculate
        introducedInLesson: bond-options.european-bond-options
    - key: bond-call-expiry-payoff
      latex: 'X^{B,\mathrm{call}}_{T_{\mathrm{opt}}}'
      meaning: Non-negative holder payoff from buying the bond at the dirty strike when economically beneficial.
      units: stated currency per option at expiry
      sources:
        - id: hull-options-futures
          locator: 'Ch. 28 §28.1, printed pp. 648-650, Eqs. 28.1-28.3 (European bond options on cash or dirty bond value with a cash strike).'
      alignment:
        kind: competency
        introducedByCompetency: bond-options.european-value.calculate
        introducedInLesson: bond-options.european-bond-options
    - key: bond-put-expiry-payoff
      latex: 'X^{B,\mathrm{put}}_{T_{\mathrm{opt}}}'
      meaning: Non-negative holder payoff from selling the bond at the dirty strike when economically beneficial.
      units: stated currency per option at expiry
      sources:
        - id: hull-options-futures
          locator: 'Ch. 28 §28.1, printed pp. 648-650, Eqs. 28.1-28.3 (European bond options on cash or dirty bond value with a cash strike).'
      alignment:
        kind: competency
        introducedByCompetency: bond-options.european-value.calculate
        introducedInLesson: bond-options.european-bond-options
---

import AssessmentSet from 'explico/components/assessments/AssessmentSet.astro';
import Diagram from 'explico/components/diagram/Diagram.astro';

## The standalone contract

A European bond call gives the holder the right, but not the obligation, to
buy a named bond at [[option-expiry-time]]. A put gives the right to sell.
This option is not the issuer's call feature inside a callable bond. The two
rights are held by different parties and belong to different instruments.

## Put the bond value and the strike on one price basis

The local [[bond-option-expiry-value]]
$B_{T_{\mathrm{opt}}}^{\mathrm{dirty}}$ and [[option-strike-price]] $K$
must both be dirty cash prices. If a contract states a clean strike, add the
[[accrued-interest]] at the exercise date to the strike before the comparison.

In this lesson, exercise occurs immediately after any coupon paid on the expiry
date. The call payoff and the put payoff are:

$$
\explain{bond-call-expiry-payoff}{X^{B,\mathrm{call}}_{T_{\mathrm{opt}}}}
=\max\left(
\explain{bond-option-expiry-value}{B_{T_{\mathrm{opt}}}^{\mathrm{dirty}}}
-\explain{option-strike-price}{K},0\right),
\label{eq:bond-call-expiry-payoff}
$$

$$
\explain{bond-put-expiry-payoff}{X^{B,\mathrm{put}}_{T_{\mathrm{opt}}}}
=\max\left(
\explain{option-strike-price}{K}
-\explain{bond-option-expiry-value}{B_{T_{\mathrm{opt}}}^{\mathrm{dirty}}},0\right).
\label{eq:bond-put-expiry-payoff}
$$

The lattice first gives the bond values at expiry. Apply
[[eq-bond-call-expiry-payoff]] or [[eq-bond-put-expiry-payoff]] at the expiry
nodes. Then apply backward induction to calculate the [[lattice-node-value]]
at time zero.

<Diagram
  id="bond-option-valuation-flow"
  label="European bond-option valuation flow"
  direction="LR"
  nodes={[
    { id: 'bond-tree', text: 'Value the bond to option expiry' },
    { id: 'payoff', text: 'Calculate the expiry payoff' },
    { id: 'induction', text: 'Discount expected option values backward' },
  ]}
  edges={[
    { from: 'bond-tree', to: 'payoff' },
    { from: 'payoff', to: 'induction' },
  ]}
  description="First value the bond at every option-expiry node, then apply the call or put payoff, then discount the risk-neutral expected payoffs backward through the input lattice."
/>

## Worked example

Suppose a one-period input lattice gives dirty bond values at expiry of USD 90
and USD 110. The strike is USD 100, the risk-neutral up weight is 0.60, and the
discount factor is 0.95. Applying [[eq-bond-call-expiry-payoff]] and one
backward step gives the call value:

$$
\begin{aligned}
\explain{bond-call-expiry-payoff}{X_{T_{\mathrm{opt}}}^{B,\mathrm{call},\mathrm{down}}}
&=\max(90-100,0)=0,\\
\explain{bond-call-expiry-payoff}{X_{T_{\mathrm{opt}}}^{B,\mathrm{call},\mathrm{up}}}
&=\max(110-100,0)=10,\\
\explain{lattice-node-value}{V_0}
&=0.95\left[(1-0.60)(0)+0.60(10)\right]\\
&=5.70\ \text{USD per option}.
\end{aligned}
$$

The tested domain code does the same two steps: `europeanOptionPayoff`
calculates the expiry payoffs, and `backwardInductionValue` discounts them.

## Coupon and exercise ordering

In this lesson, a coupon on the expiry date is paid before exercise. So the
bond value at expiry is ex-coupon. A contract that is exercised before the
coupon, or that transfers the right to the coupon, requires different terminal
inputs.

## Check your understanding

<AssessmentSet />

## Sources

The bond-option definition and the need to distinguish a cash (dirty) strike
from a quoted (clean) strike follow Hull, Chapter 28 §28.1 [@hull-options-futures].
This lesson uses an input lattice, not Black's market model, so that the
next lesson can model the issuer-default state explicitly.
