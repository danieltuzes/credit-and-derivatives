---
title: Issuer-default knockout bond options
description: Value a bond option that is extinguished by issuer default while keeping bond recovery separate.
editorialStatus: draft
teaches:
  - bond-options.default-knockout.interpret
  - bond-options.default-knockout-value.calculate
assumptions:
  - Standalone European option on one defaultable fixed-cash-flow bond
  - Issuer default in any lattice period before exercise extinguishes the option with zero option rebate
  - The underlying bond may pay a separate input non-negative recovery cash amount at the next lattice date after period default
  - Exercise occurs immediately after any scheduled surviving bond cash flow at expiry; strike and alive bond values are dirty cash amounts
  - Input risk-neutral up weights, conditional survival probabilities, recoveries, and one-period discount factors; no calibration or real-world forecast
  - Conditional survival/default is applied at the current node before the next rate-state branch; no simultaneous-event ambiguity inside a period
  - No counterparty default, early exercise, option rebate, restructuring, settlement delay, correlation calibration, liquidity, tax, or funding adjustment
notation:
  local:
    - key: knockout-node-discount-factor
      latex: 'd_{i,j}'
      meaning: One-period discount factor from the current alive node to the next lattice time.
      units: current-node currency per next-time currency
      sources:
        - id: hull-options-futures
          locator: 'Ch. 12 §12.3, printed pp. 259-261, Eq. 12.5 and Eqs. 12.7-12.10 (one-step discounting in backward induction).'
      alignment:
        kind: competency
        introducedByCompetency: bond-options.default-knockout-value.calculate
        introducedInLesson: bond-options.issuer-default-knockout
    - key: knockout-node-up-weight
      latex: 'q_{i,j}'
      meaning: Conditional pricing probability assigned to the next up rate state if the issuer survives.
      units: probability between zero and one
      sources:
        - id: hull-options-futures
          locator: 'Ch. 12 §12.3, printed pp. 259-261, Eq. 12.6 and Eqs. 12.7-12.10 (risk-neutral up weight in backward induction).'
      alignment:
        kind: competency
        introducedByCompetency: bond-options.default-knockout-value.calculate
        introducedInLesson: bond-options.issuer-default-knockout
    - key: surviving-bond-next-cash-flow
      latex: 'CF^{B}_{i+1}'
      meaning: Coupon or coupon-plus-redemption paid at the next lattice time only if the issuer survives the period.
      units: stated currency per bond at the next time
      sources:
        - id: tuckman-serrat-fixed-income
          locator: 'Ch. 1 §1.1, printed p. 50 and Table 1.1 (scheduled coupon and principal cash flows); Ch. 14 §14.7, printed pp. 371-372 and Table 14.11 (bond cash flows separated into default and no-default states).'
      alignment:
        kind: competency
        introducedByCompetency: bond-options.default-knockout-value.calculate
        introducedInLesson: bond-options.issuer-default-knockout
    - key: bond-default-recovery-cash
      latex: 'R_{i,j}^{B}'
      meaning: Non-negative recovery paid on the underlying bond at the next lattice date after default in the period.
      units: stated currency per bond at the next time
      sources:
        - id: tuckman-serrat-fixed-income
          locator: 'Ch. 14 §14.7, printed pp. 371-372, Table 14.11 and Eq. 14.9 (defaulted bond principal recovery as face amount times recovery rate).'
      seeAlso:
        - recovery-rate
      alignment:
        kind: competency
        introducedByCompetency: bond-options.default-knockout-value.calculate
        introducedInLesson: bond-options.issuer-default-knockout
    - key: surviving-bond-continuation-value
      latex: 'H^B_{i,j}'
      meaning: Next-time scheduled bond cash plus risk-neutral expected ex-cash-flow bond value, conditional on period survival.
      units: stated currency per bond at the next time
      sources:
        - id: hull-options-futures
          locator: 'Ch. 12 §12.3, printed pp. 259-261, Eqs. 12.7-12.10 (backward induction from successor claim values and terminal payoffs).'
        - id: tuckman-serrat-fixed-income
          locator: 'Ch. 7 §7.3, printed pp. 182-184, Eqs. 7.7-7.8 (one-period risk-neutral expected-discounted valuation); Ch. 14 §14.7, printed pp. 371-372, Table 14.11 and Eqs. 14.8-14.10 (survival- and default-contingent bond cash flows).'
      alignment:
        kind: competency
        introducedByCompetency: bond-options.default-knockout-value.calculate
        introducedInLesson: bond-options.issuer-default-knockout
---

import AssessmentSet from 'explico/components/assessments/AssessmentSet.astro';
import Diagram from 'explico/components/diagram/Diagram.astro';

## Contract definition

In this lesson, “knockout” has one trigger: if the issuer defaults before the
exercise time, the option is extinguished and pays no rebate. This knockout is
not a barrier on the market price of the bond.

The underlying bond is a different claim. The bond can still pay recovery after
default. So the recovery is part of the bond value, and it is never part of the
option value in the default state.

## Value the underlying bond first

An alive node is a node at which the issuer has not defaulted. At an alive node
with [[lattice-time-index]] $i$ and [[lattice-state-index]] $j$, use the
local node discount factor $d_{i,j}$, the local risk-neutral up weight
$q_{i,j}$, the [[conditional-node-survival-probability]] $s_{i,j}$, the next
surviving cash flow $CF^B_{i+1}$, and the local bond recovery $R^B_{i,j}$.
The local [[surviving-bond-continuation-value]] is the bond value at the next
time, conditional on survival. It is the next surviving cash flow plus the
expected alive bond value:

$$
\explain{surviving-bond-continuation-value}{H^B_{i,j}}
=\explain{surviving-bond-next-cash-flow}{CF^B_{i+1}}
+(1-\explain{knockout-node-up-weight}{q_{i,j}})
\explain{alive-bond-value}{B^{\mathrm{alive}}_{i+1,j}}
+\explain{knockout-node-up-weight}{q_{i,j}}
\explain{alive-bond-value}{B^{\mathrm{alive}}_{i+1,j+1}}.
\label{eq:surviving-bond-continuation}
$$

The [[alive-bond-value]] is the discounted expectation over survival and
default. Survival gives the continuation value from
[[eq-surviving-bond-continuation]], and default gives the recovery:

$$
\explain{alive-bond-value}{B^{\mathrm{alive}}_{i,j}}
=\explain{knockout-node-discount-factor}{d_{i,j}}
\left[
\explain{conditional-node-survival-probability}{s_{i,j}}
\explain{surviving-bond-continuation-value}{H^B_{i,j}}
+(1-\explain{conditional-node-survival-probability}{s_{i,j}})
\explain{bond-default-recovery-cash}{R^B_{i,j}}
\right].
\label{eq:alive-bond-recursion}
$$

The values at each node are ex-cash-flow. The model assumes that recovery is
paid at the next lattice date. This timing is a simplification.

## Apply the knockout to option continuation

At call expiry, if the issuer has not defaulted, the option value is the call
payoff:

$$
\explain{knockout-bond-option-value}{O^{\mathrm{KO}}_{T,j}}
=\max\left(
\explain{alive-bond-value}{B^{\mathrm{alive}}_{T,j}}
-\explain{option-strike-price}{K},0\right).
\label{eq:knockout-terminal-payoff}
$$

Before expiry, the option value in the default state is zero. So the option
value at a node is the discounted, survival-weighted expectation of the option
values at the two successor nodes:

$$
\explain{knockout-bond-option-value}{O^{\mathrm{KO}}_{i,j}}
=\explain{knockout-node-discount-factor}{d_{i,j}}
\explain{conditional-node-survival-probability}{s_{i,j}}
\left[
(1-\explain{knockout-node-up-weight}{q_{i,j}})
\explain{knockout-bond-option-value}{O^{\mathrm{KO}}_{i+1,j}}
+\explain{knockout-node-up-weight}{q_{i,j}}
\explain{knockout-bond-option-value}{O^{\mathrm{KO}}_{i+1,j+1}}
\right].
\label{eq:knockout-option-recursion}
$$

[[eq-knockout-option-recursion]] has no recovery term, unlike
[[eq-alive-bond-recursion]]. If the conditional survival probability at the
current node is zero, the option value is zero, also when the bond recovery is
positive.

<Diagram
  id="knockout-bond-option-flow"
  label="Issuer-default knockout valuation flow"
  direction="LR"
  nodes={[
    { id: 'bond', text: 'Value the alive bond with recovery' },
    { id: 'terminal', text: 'Apply the alive-state option payoff' },
    {
      id: 'option',
      text: 'Apply backward induction with zero option value at default',
    },
  ]}
  edges={[
    { from: 'bond', to: 'terminal' },
    { from: 'terminal', to: 'option' },
  ]}
  description="Value the underlying bond first, including its separate input recovery. At option expiry, apply the payoff only in alive states. Before expiry, discount only the survival-weighted option values, because the option value at default is zero."
/>

## Worked example

Consider a lattice with two annual steps. At option expiry, the input values of
the final period give alive bond values of USD 82.80 and USD 88.65. With a call
strike of USD 85, [[eq-knockout-terminal-payoff]] gives terminal option values
of USD 0 and USD 3.65. At valuation time, the discount factor is 0.95, the
conditional survival probability is 0.90, and the up weight is 0.50. The option
values are:

$$
\begin{aligned}
O^{\mathrm{KO}}_{1,0}&=\max(82.80-85,0)=0,\\
O^{\mathrm{KO}}_{1,1}&=\max(88.65-85,0)=3.65,\\
O^{\mathrm{KO}}_{0,0}
&=0.95(0.90)\left[(1-0.50)(0)+0.50(3.65)\right]\\
&=1.560375\ \text{USD per option}.
\end{aligned}
$$

The domain function `valueDefaultKnockoutBondOption` performs the same bond
recursion and option recursion, in the same order.

## Put-call parity with a default knockout

Put-call parity for options without a knockout does not hold unchanged for this
contract. In a default state before expiry, the call value minus the put value
is zero. So the matching synthetic forward must also be extinguished by the
same default event. A default-free forward does not match the options in every
state.

## Model and source boundary

Hull describes European bond options. Separately, Hull describes CDS forwards
and CDS options that cease to exist if the reference entity defaults. This
lesson combines the two descriptions for teaching: an option on the issuer's
bond that is knocked out by issuer default. The lesson implements the stated
contract. It does not claim that all bond options use this convention. The
joint lattice for rates and credit is an input; it is not calibrated.

## Check your understanding

<AssessmentSet />

## Sources

The bond-option cash-price conventions follow Hull, Chapter 28 §28.1. Hull
describes the analogous extinguishment at default for CDS forwards and CDS
options in Chapter 24 §24.5. Backward induction follows Chapter 12
[@hull-options-futures]. A recovery convention must state its base and its
timing, as the fixed-income treatment emphasizes [@tuckman-serrat-fixed-income].
