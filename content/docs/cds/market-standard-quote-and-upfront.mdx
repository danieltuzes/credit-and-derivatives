---
title: CDS market-standard quote and upfront
description: Distinguish a conventional CDS spread quote from a fixed running coupon, identify which quantity is observed and which is solved, calculate the signed upfront amount, and reverse the simplified conversion.
editorialStatus: draft
teaches:
  - cds.market-standard-quote.interpret
  - cds.upfront-amount.calculate
  - cds.quote-upfront-conversion.calculate
assumptions:
  - The named perspective is a protection buyer that pays running premium, receives protection, and pays a positive signed upfront amount
  - MSQ is a lesson-local abbreviation for market-standard quote, meaning the conventional quoted spread that is an input to this simplified converter
  - The MSQ is treated as a zero-upfront par spread that implies one non-negative constant risk-neutral hazard rate
  - The market level is an input, stated either as an MSQ or as a signed upfront; the converter gives the mapping between the two and does not choose the market level
  - Within one internally consistent model, the MSQ returned from an upfront amount equals that model's zero-upfront par spread
  - The running coupon is fixed and may differ from the MSQ
  - The premium annuity includes scheduled premium and exact accrued premium at modeled default time under the preceding CDS lesson's flat-hazard model
  - The model uses equal annual, semiannual, or quarterly model-year periods, one deterministic continuously compounded risk-free rate, and one deterministic recovery rate
  - The signed upfront amount is exchanged at valuation time zero rather than a later cash-settlement date
  - The reverse conversion solves for hazard because both the premium annuity and protection value depend on the quote-implied hazard rate
  - The model has no calendar dates, day-count engine, business-day adjustments, curve bootstrapping, settlement delay, counterparty risk, collateral, funding, or volatility parameter
---

import CompactExample from 'explico/components/examples/CompactExample.astro';
import CompactExamples from 'explico/components/examples/CompactExamples.astro';
import AssessmentSet from 'explico/components/assessments/AssessmentSet.astro';

## What you will be able to do

After this lesson, you should be able to:

- distinguish a conventional spread quote from the contract's fixed running
  coupon;
- identify which quantity is an input and which is solved in each quotation
  direction;
- calculate the signed upfront amount that makes protection-buyer inception
  value zero;
- reverse the simplified conversion without incorrectly freezing a
  quote-dependent premium annuity.

## Three quantities with three different roles

Standardized CDS quotation separates a fixed running coupon from a
market-determined upfront amount. Textbook treatments describe how to convert a
quoted spread, treated as a par spread, into the value of a contract with a
fixed coupon. The official converter specification accepts either the spread or
the upfront and returns the other. [@tuckman-serrat-fixed-income; Ch. 14 §14.6, printed pp. 366-371; Appendix A14.2, printed pp. 506-507, eqs. A14.5-A14.7]
[@hull-options-futures; Ch. 24 §24.4, printed pp. 556-557]
[@isda-cds-standard-model; Standard CDS Contract Converter Specification, version May 5, 2009, Functionality and Specification, printed p. 1]

This lesson uses the name **market-standard quote** (**MSQ**) for this
conventional quoted spread. The name belongs to this course. The books and the
official specification more commonly say _quoted spread_, _conventional
spread_, or _spread_. This lesson does not claim that “MSQ” is a universal
abbreviation.

The three quantities have different roles, but they are not three independent
inputs:

- the [[cds-market-standard-quote]]
  $\explain{cds-market-standard-quote}{s_{\mathrm{MSQ}}}$ is treated as a
  zero-upfront [[cds-par-spread]] and calibrates this lesson's flat
  [[hazard-rate]] $\lambda$;
- the [[cds-standard-coupon]]
  $\explain{cds-standard-coupon}{c_{\mathrm{std}}}$ determines the running
  premium cash flows;
- the [[cds-upfront-amount]]
  $\explain{cds-upfront-amount}{U_0}$ offsets the difference in value at
  inception.

The phrase “upfront fee” is also common. This lesson uses **upfront amount**,
because the amount is signed. The sign of $U_0$ means:

- $U_0>0$: the protection buyer pays;
- $U_0=0$: no upfront cash changes hands in the toy model;
- $U_0<0$: the protection buyer receives $-U_0$.

In the formulas, rates are decimals per year. Convert a [[basis-point]] quote
to decimal form by dividing by 10,000.

## First decide which quantities are inputs and which are solved

The equation that connects the par spread and the hazard rate can be used in
two directions. So a quantity can be an input in one calculation and solved in
another. The table shows the inputs and the solved quantities of each
calculation.

| Calculation      | Input                                                                                              | Solved or calculated                                  |
| ---------------- | -------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| Model par spread | Hazard rate, recovery, discount input, maturity, and cash-flow conventions                         | Par spread $s^{\star}$                                |
| MSQ to upfront   | Market quote $s_{\mathrm{MSQ}}$, standard coupon $c_{\mathrm{std}}$, and the converter conventions | Flat hazard rate $\lambda$, then signed upfront $U_0$ |
| Upfront to MSQ   | Market upfront $U_0$, standard coupon $c_{\mathrm{std}}$, and the converter conventions            | Flat hazard rate $\lambda$, then $s_{\mathrm{MSQ}}$   |

If the hazard rate is an input, the par spread is the output:

$$
\explain{cds-par-spread}{s^{\star}}
=
\frac{
\explain{cds-protection-leg-present-value}{PV_0^{\mathrm{prot}}}(\explain{hazard-rate}{\lambda})
}{
\explain{cds-notional}{N}
\explain{cds-premium-annuity}{A_0^{\mathrm{prem}}}(\explain{hazard-rate}{\lambda})
}.
\label{eq:cds-par-spread-from-hazard}
$$

If the MSQ is the input instead, [[eq-cds-par-spread-from-hazard]] is a
calibration equation: the quoted spread is the input, and the flat hazard rate
is the unknown. If the upfront amount is the input, the upfront balance is the
calibration equation. The market participant or the price source provides the
market level. The standard converter specifies how the spread and the upfront
are converted into each other under common assumptions. [@isda-cds-standard-model; Standard CDS Contract Converter Specification, version May 5, 2009, Functionality and Specification, printed pp. 1 and 4]

So do not fix both $s_{\mathrm{MSQ}}$ and $\lambda$ independently. When the
other model inputs are fixed, $s_{\mathrm{MSQ}}$ and $\lambda$ must together
satisfy the zero-upfront condition.

## Why one upfront amount is enough at inception

For fixed model inputs, one positive [[cds-protection-leg-present-value]]
summarizes all future protection cash flows. One positive
[[cds-premium-leg-present-value]] summarizes all fixed-coupon cash flows. The
difference of the two present values is one currency amount. So one cash
transfer at time zero can offset that difference exactly. The upfront balance
is:

$$
\explain{cds-protection-leg-present-value}{PV_0^{\mathrm{prot}}}
=
\explain{cds-premium-leg-present-value}{PV_0^{\mathrm{prem}}}
+\explain{cds-upfront-amount}{U_0}.
\label{eq:cds-upfront-balance}
$$

Equivalently, the value to the protection buyer at inception, after the signed
upfront amount, is zero:

$$
0=
\explain{cds-protection-leg-present-value}{PV_0^{\mathrm{prot}}}
-\explain{cds-premium-leg-present-value}{PV_0^{\mathrm{prem}}}
-\explain{cds-upfront-amount}{U_0}.
$$

This is a value identity, not a claim that the CDS becomes riskless. After
inception its value can change when credit, interest-rate, recovery, liquidity,
or other inputs change.

## Risk-neutral does not mean risk-free

The calibration uses **risk-neutral pricing probabilities**. They are not
necessarily real-world default forecasts. Under risk-neutral valuation,
probabilities are pricing weights, chosen consistently with traded prices and
risk-free discounting. “Risk-free” describes the discount input, not the
probability measure. So the hazard rate inferred from prices can differ from an
estimated real-world default intensity. [@tuckman-serrat-fixed-income; Ch. 7 §7.3, printed pp. 182-184, eqs. 7.7-7.8; Ch. 14 §14.7, printed p. 371]
[@hull-options-futures; Ch. 23 §23.5, printed pp. 528-530]

A change of probability measure does not remove volatility. This converter has
no separate volatility input, for two reasons. Its discount rate, hazard rate,
recovery rate, and schedule are deterministic. Its leg values are expectations
that are linear in the one-default cash flows. A model with stochastic rates,
stochastic default intensity, stochastic recovery, wrong-way dependence, or CDS
options can require volatility and correlation inputs. The risk-neutral measure
gives pricing weights; it does not remove those risks.

## Convert MSQ to signed upfront

The conversion must use one internally consistent model. For each candidate
constant hazard rate, calculate the positive
[[cds-premium-annuity]]
$\explain{cds-premium-annuity}{A_0^{\mathrm{prem}}}(\explain{hazard-rate}{\lambda})$
and the protection-leg present value. First, solve the zero-upfront quote
condition for $\lambda$:

$$
\explain{cds-market-standard-quote}{s_{\mathrm{MSQ}}}
\explain{cds-notional}{N}
\explain{cds-premium-annuity}{A_0^{\mathrm{prem}}}(\explain{hazard-rate}{\lambda})
=
\explain{cds-protection-leg-present-value}{PV_0^{\mathrm{prot}}}(\explain{hazard-rate}{\lambda}).
\label{eq:cds-msq-calibration}
$$

The root $\lambda$ of [[eq-cds-msq-calibration]] is the flat risk-neutral
hazard rate implied by the MSQ, under the assumptions of this lesson on the
schedule, discounting, recovery, and accrual at default. The premium-leg
present value with the fixed coupon is then:

$$
\explain{cds-premium-leg-present-value}{PV_0^{\mathrm{prem}}}
(\explain{cds-standard-coupon}{c_{\mathrm{std}}};\explain{hazard-rate}{\lambda})
=
\explain{cds-standard-coupon}{c_{\mathrm{std}}}
\explain{cds-notional}{N}
\explain{cds-premium-annuity}{A_0^{\mathrm{prem}}}(\explain{hazard-rate}{\lambda}).
\label{eq:cds-fixed-coupon-premium}
$$

Substituting [[eq-cds-msq-calibration]] and [[eq-cds-fixed-coupon-premium]]
into the upfront balance [[eq-cds-upfront-balance]] gives the upfront amount:

$$
\begin{aligned}
\explain{cds-upfront-amount}{U_0}
&=
\explain{cds-protection-leg-present-value}{PV_0^{\mathrm{prot}}}(\explain{hazard-rate}{\lambda})
-
\explain{cds-standard-coupon}{c_{\mathrm{std}}}
\explain{cds-notional}{N}
\explain{cds-premium-annuity}{A_0^{\mathrm{prem}}}(\explain{hazard-rate}{\lambda})\\
&=
\explain{cds-notional}{N}
\explain{cds-premium-annuity}{A_0^{\mathrm{prem}}}(\explain{hazard-rate}{\lambda})
\left(
\explain{cds-market-standard-quote}{s_{\mathrm{MSQ}}}
-\explain{cds-standard-coupon}{c_{\mathrm{std}}}
\right).
\end{aligned}
\label{eq:cds-upfront-from-msq}
$$

The substitution in [[eq-cds-upfront-from-msq]] is valid only because every
term uses the same model and the same calibrated hazard rate. The premium
annuity and the notional are positive. So an MSQ above the fixed coupon gives a
positive upfront amount, which the buyer pays. An MSQ below the fixed coupon
gives a negative upfront amount, which the buyer receives.

## Convert signed upfront back to MSQ

When the input is a signed upfront amount, do not rearrange the last line of
[[eq-cds-upfront-from-msq]] with the annuity held fixed. In this flat-hazard
converter, both the protection-leg present value and the premium annuity change
with the unknown hazard rate. Instead, solve this equation for the hazard rate:

$$
\explain{cds-upfront-amount}{U_0}
=
\explain{cds-protection-leg-present-value}{PV_0^{\mathrm{prot}}}(\explain{hazard-rate}{\lambda})
-
\explain{cds-standard-coupon}{c_{\mathrm{std}}}
\explain{cds-notional}{N}
\explain{cds-premium-annuity}{A_0^{\mathrm{prem}}}(\explain{hazard-rate}{\lambda})
\label{eq:cds-upfront-to-hazard}
$$

Solve [[eq-cds-upfront-to-hazard]] for a non-negative $\lambda$. Then
[[eq-cds-hazard-to-msq]] gives the MSQ from this $\lambda$:

$$
\explain{cds-market-standard-quote}{s_{\mathrm{MSQ}}}
=
\frac{
\explain{cds-protection-leg-present-value}{PV_0^{\mathrm{prot}}}(\explain{hazard-rate}{\lambda})
}{
\explain{cds-notional}{N}
\explain{cds-premium-annuity}{A_0^{\mathrm{prem}}}(\explain{hazard-rate}{\lambda})
}.
\label{eq:cds-hazard-to-msq}
$$

If a curve and its annuity have already been calibrated and are held fixed,
rearranging [[eq-cds-upfront-from-msq]] gives a shortcut:

$$
\explain{cds-market-standard-quote}{s_{\mathrm{MSQ}}}
=
\explain{cds-standard-coupon}{c_{\mathrm{std}}}
+
\frac{
\explain{cds-upfront-amount}{U_0}
}{
\explain{cds-notional}{N}
\explain{cds-premium-annuity}{A_0^{\mathrm{prem}}}(\explain{hazard-rate}{\lambda})
}.
$$

This shortcut is not the full conversion from upfront to quote when the quote
itself determines $\lambda$, because then the denominator changes with the
quote.

## When MSQ and par spread are equal

Within one internally consistent risk-neutral model, the MSQ and the par spread
are equal. After the converter has inferred one $\lambda$, from the spread or
from the upfront amount, the following quantities are equal:

$$
\begin{aligned}
\explain{cds-market-standard-quote}{s_{\mathrm{MSQ}}}
&=
\explain{cds-par-spread}{s^{\star}}\\
&=
\frac{
\explain{cds-protection-leg-present-value}{PV_0^{\mathrm{prot}}}(\explain{hazard-rate}{\lambda})
}{
\explain{cds-notional}{N}
\explain{cds-premium-annuity}{A_0^{\mathrm{prem}}}(\explain{hazard-rate}{\lambda})
}\\
&=
\explain{cds-standard-coupon}{c_{\mathrm{std}}}
+
\frac{
\explain{cds-upfront-amount}{U_0}
}{
\explain{cds-notional}{N}
\explain{cds-premium-annuity}{A_0^{\mathrm{prem}}}(\explain{hazard-rate}{\lambda})
}.
\end{aligned}
\label{eq:cds-msq-par-equality}
$$

The equality in [[eq-cds-msq-par-equality]] is a risk-neutral pricing identity.
No real-world default probability enters it. The equality requires the same
pricing probabilities, discount inputs, recovery, schedule, default-accrual
treatment, and settlement convention in every term.

The phrase **par spread from another curve** needs more care. The observed
trade with a standard coupon gives one combined price:

$$
\explain{cds-upfront-amount}{U_0}
=
\explain{cds-protection-leg-present-value}{PV_0^{\mathrm{prot}}}
-
\explain{cds-standard-coupon}{c_{\mathrm{std}}}
\explain{cds-notional}{N}
\explain{cds-premium-annuity}{A_0^{\mathrm{prem}}}.
$$

This price does not show the protection-leg present value and the premium
annuity separately. The standard converter separates them by assuming its
constant-hazard model. A different risk-neutral term structure can reprice the
same contract, with its fixed coupon and upfront amount, but with a different
premium annuity. That term structure then gives a different par coupon to a
hypothetical zero-upfront contract. The official converter itself distinguishes
its single constant hazard rate from the term structure of a fuller
credit-curve model. [@isda-cds-standard-model; Standard CDS Examples, version October 26, 2012, printed p. 2 (the converter uses a single flat hazard rate rather than a term structure of flat spreads)]

This difference is not an arbitrage by itself, because the observed
fixed-coupon CDS and the hypothetical zero-upfront CDS have different premium
cash flows. No arbitrage requires identical tradable cash flows to have the
same price. But suppose a zero-upfront CDS that pays $s_{\mathrm{MSQ}}$ could be
traded on otherwise identical terms. Then a curve calibrated consistently to
that traded contract would have to give it zero value. [@tuckman-serrat-fixed-income; Ch. 1 §§1.3-1.4, printed pp. 53-57 (law of one price, replication, and arbitrage enforcement)]

<CompactExamples
  summary="Worked conversions"
  hint="Open the set, then compare calibration, running premium, protection, and upfront."
  tabsLabel="CDS quote and upfront examples"
>
  <CompactExample label="MSQ to buyer-paid upfront">

Consider a five-year CDS with quarterly premiums and notional
$\explain{cds-notional}{N}=\text{USD }10{,}000{,}000$, 2% continuously
compounded risk-free rate, 40% recovery, MSQ
$\explain{cds-market-standard-quote}{s_{\mathrm{MSQ}}}=0.015$ per year (150
basis points per year), and fixed coupon
$\explain{cds-standard-coupon}{c_{\mathrm{std}}}=0.010$ per year (100 basis
points per year).

Under the flat-hazard model of this lesson, with exact default times, the
calibration to the quote gives:

$$
\explain{hazard-rate}{\lambda}=0.024937617\text{ per year},
\qquad
\explain{cds-premium-annuity}{A_0^{\mathrm{prem}}}(\explain{hazard-rate}{\lambda})
=4.466915290\text{ model-years}.
$$

Because the calibrated quote is a par spread, the protection-leg present value
is:

$$
\explain{cds-protection-leg-present-value}{PV_0^{\mathrm{prot}}}
=0.015(10{,}000{,}000)(4.466915290)
=\text{USD }670{,}037.29.
$$

The premium-leg present value with the fixed coupon is:

$$
\explain{cds-premium-leg-present-value}{PV_0^{\mathrm{prem}}}
=0.010(10{,}000{,}000)(4.466915290)
=\text{USD }446{,}691.53.
$$

The upfront amount is the difference of the two present values:

$$
\explain{cds-upfront-amount}{U_0}
=670{,}037.29-446{,}691.53
=\text{USD }223{,}345.76.
$$

The upfront amount is positive, so the protection buyer pays it. As a check,
$670{,}037.29-446{,}691.53-223{,}345.76=0$, up to the rounding of the
displayed values.

  </CompactExample>

  <CompactExample label="Why reverse conversion is a solve">

Suppose the input for the same contract is the signed upfront amount
$\explain{cds-upfront-amount}{U_0}=\text{USD }223{,}345.76$, and the MSQ is
unknown. A solver changes the flat $\lambda$ and recalculates both the
protection-leg present value and the premium annuity, until
[[eq-cds-upfront-to-hazard]] holds.

The root is approximately $\lambda=0.024937617$ per year. Only with this root
does [[eq-cds-hazard-to-msq]] give $s_{\mathrm{MSQ}}\approx0.015$ (150 basis
points per year).

The shortcut with the annuity fixed at $4.466915290$ gives the same answer
here, but only because this annuity was already calculated at the solution's
hazard rate. Before the solve, this annuity is unknown.

  </CompactExample>
</CompactExamples>

## What a real standard converter adds

The official converter uses dates and market conventions. Its inputs include
the trade date and the maturity date, the notional, the standard coupon, the
recovery rate, the discount inputs, and either the spread or the upfront. Its
outputs include the other quotation and a cash-settlement amount.
[@isda-cds-standard-model; Standard CDS Contract Converter Specification, version May 5, 2009, Functionality and Specification, printed p. 1]

This lesson is not an implementation of that standard model. It uses
model-year periods and a single flat hazard rate. A production conversion must
also handle calendar schedules, day-count and business-day conventions, curve
construction and interpolation, settlement timing, the accrued amount, and
other contract details. The identities of this lesson explain why the quote,
the coupon, and the upfront amount differ. But the numbers in this lesson are
not cash-settlement instructions for a trade.

## Check your understanding

The assessment separates quote interpretation, signed upfront arithmetic, and
the reverse flat-hazard solve. Each competency has direct and transfer
evidence.

<AssessmentSet />

## Model boundary and review note

This AI-assisted lesson and all cited locators remain `draft` pending human
editorial and quantitative review. All input values are invented for teaching. The converter assumes
one deterministic discount rate, one deterministic recovery rate, one
constant risk-neutral hazard rate, regular model-year periods, exact
default-time accrued premium inside that toy schedule, and an upfront payment
at time zero. It omits the contract details and numerical methods listed above,
as well as stochastic rates, stochastic intensity or recovery, volatility and
correlation calibration, counterparty risk, collateral, funding, liquidity,
and transaction costs.
