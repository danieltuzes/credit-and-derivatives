---
title: Multi-period lattice valuation
description: Extend one-period risk-neutral valuation to node-by-node backward induction with local discount factors.
editorialStatus: draft
teaches:
  - finance.backward-induction.calculate
assumptions:
  - An input recombining pricing lattice with two successor nodes from each current node
  - Strictly positive one-period discount factors and risk-neutral up weights between zero and one inclusive
  - Terminal values and any next-date cash flows share one currency and holder sign convention
  - No claim that the input lattice has been calibrated or is complete outside its modeled states
notation:
  local:
    - key: lattice-node-discount-factor
      latex: 'd_{i,j}'
      meaning: Currency at node i,j per one unit of currency at either successor node one step later.
      units: current-node currency per next-time currency
      sources:
        - id: hull-options-futures
          locator: 'Ch. 12 §12.3, printed pp. 259-261, Eq. 12.5 and Eqs. 12.7-12.10 (one-step discounting in backward induction).'
      alignment:
        kind: competency
        introducedByCompetency: finance.backward-induction.calculate
        introducedInLesson: derivatives.multiperiod-lattice-valuation
    - key: lattice-node-up-weight
      latex: 'q_{i,j}'
      meaning: Pricing probability assigned to the up successor conditional on the current node.
      units: probability between zero and one
      sources:
        - id: hull-options-futures
          locator: 'Ch. 12 §12.3, printed pp. 259-261, Eq. 12.6 and Eqs. 12.7-12.10 (risk-neutral up weight in backward induction).'
      seeAlso:
        - risk-neutral-probability-measure
      alignment:
        kind: competency
        introducedByCompetency: finance.backward-induction.calculate
        introducedInLesson: derivatives.multiperiod-lattice-valuation
    - key: lattice-next-cash-flow
      latex: 'CF_{i+1,j}'
      meaning: Signed cash paid at a successor node immediately before its continuation value is measured.
      units: stated currency at the successor time
      sources:
        - id: hull-options-futures
          locator: 'Ch. 12 §12.3, printed pp. 259-261, Eqs. 12.7-12.10 (backward induction from successor claim values and terminal payoffs).'
      alignment:
        kind: competency
        introducedByCompetency: finance.backward-induction.calculate
        introducedInLesson: derivatives.multiperiod-lattice-valuation
---

import AssessmentSet from 'explico/components/assessments/AssessmentSet.astro';
import Diagram from 'explico/components/diagram/Diagram.astro';

## Repeat the one-period argument backward

The one-period binomial lesson valued two terminal payoffs. A multi-period
lattice applies the same pricing step at every node, beginning with the last
row and moving backward to [[valuation-time]].

At the node with [[lattice-time-index]] $i$ and [[lattice-state-index]] $j$,
let the local [[lattice-node-up-weight]] be $q_{i,j}$ and the local
[[lattice-node-discount-factor]] be $d_{i,j}$. Let the local next-date cash
flow $\explain{lattice-next-cash-flow}{CF_{i+1,j}}$ be paid at the successor
node $(i+1,j)$; the node value $V_{i+1,j}$ excludes it. The
[[lattice-node-value]] at node $(i,j)$ is the discounted risk-neutral
expectation of the successor cash flows plus the successor node values:

$$
\explain{lattice-node-value}{V_{i,j}}
=\explain{lattice-node-discount-factor}{d_{i,j}}
\left[
(1-\explain{lattice-node-up-weight}{q_{i,j}})
\left(\explain{lattice-next-cash-flow}{CF_{i+1,j}}
+\explain{lattice-node-value}{V_{i+1,j}}\right)
+\explain{lattice-node-up-weight}{q_{i,j}}
\left(\explain{lattice-next-cash-flow}{CF_{i+1,j+1}}
+\explain{lattice-node-value}{V_{i+1,j+1}}\right)
\right].
\label{eq:lattice-backward-step}
$$

The two successors of node $(i,j)$ are the nodes $(i+1,j)$ and $(i+1,j+1)$.
The labels “up” and “down” are conventions. The input up weights and values
must use the same order.

## Timing is part of the model

In [[eq-lattice-backward-step]], each next-date cash flow is added to the value
of its successor node before discounting. So the value of a coupon bond at a
node is ex-coupon: it excludes the coupon paid at that node. A change of the
order of exercise and coupon payment changes the contract, and it can change
the option payoff.

<Diagram
  id="backward-induction-flow"
  label="Backward-induction order"
  direction="LR"
  nodes={[
    { id: 'terminal', text: 'Set terminal node values' },
    { id: 'successor', text: 'Add next-date cash flows' },
    { id: 'expectation', text: 'Take the local risk-neutral expectation' },
    { id: 'discount', text: 'Apply the local discount factor' },
    { id: 'repeat', text: 'Repeat one row earlier' },
  ]}
  edges={[
    { from: 'terminal', to: 'successor' },
    { from: 'successor', to: 'expectation' },
    { from: 'expectation', to: 'discount' },
    { from: 'discount', to: 'repeat' },
  ]}
  description="Start from terminal node values. At each earlier row, add any next-date cash flow to each successor value, form the local risk-neutral weighted average, multiply by the local discount factor, and repeat."
/>

## Worked example

The terminal values are USD 0, USD 20, and USD 40. At time index 1, both nodes
have discount factor 0.90, and their up weights are 0.25 and 0.75. At time
index 0, the discount factor is 0.95 and the up weight is 0.50. Applying
[[eq-lattice-backward-step]] row by row gives the node values:

$$
\begin{aligned}
V_{1,0}&=0.90\left[(1-0.25)(0)+0.25(20)\right]=4.50,\\
V_{1,1}&=0.90\left[(1-0.75)(20)+0.75(40)\right]=31.50,\\
V_{0,0}&=0.95\left[(1-0.50)(4.50)+0.50(31.50)\right]\\
&=17.10\ \text{USD}.
\end{aligned}
$$

`backwardInductionValue` reproduces the three node rows
`[[17.1], [4.5, 31.5], [0, 20, 40]]` in its independent reference test.

## Check your understanding

<AssessmentSet />

## Sources

The replication, risk-neutral weighting, and backward-induction construction
follow Hull, Chapter 12 §§12.1–12.3 [@hull-options-futures]. The node discount
factors are model inputs. Calibration and short-rate dynamics are outside this
lesson.
