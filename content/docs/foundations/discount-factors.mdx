---
title: Discount factors
description: Convert one deterministic future unit into its value at time zero.
editorialStatus: draft
teaches:
  - rates.discount-factor.interpret
  - rates.discount-factor.calculate
assumptions:
  - Deterministic payment of one unit
  - Non-negative nominal annual rate
  - Equal compounding periods and exact model-year time
  - Every example horizon is grid-aligned so m times t is a non-negative integer
notation:
  local:
    - key: discounted-unit-value
      latex: 'V_0(1_t)'
      meaning: 'A local reading aid that makes discount-factor units explicit. It equals one future currency unit multiplied by the discount factor for its payment time.'
      formula: 'V_0(1_t)=1_tD(0,t)'
      units: valuation currency-units per future currency-unit
      seeAlso:
        - discount-factor
        - payment-time
      alignment:
        kind: competency
        introducedByCompetency: rates.discount-factor.interpret
        introducedInLesson: foundations.discount-factors
---

import CompactExample from 'explico/components/examples/CompactExample.astro';
import CompactExamples from 'explico/components/examples/CompactExamples.astro';
import AssessmentSet from 'explico/components/assessments/AssessmentSet.astro';

## What you will be able to do

After this lesson, you should be able to:

- explain the units and meaning of a discount factor;
- calculate one from a stated nominal rate, frequency, and time;
- distinguish discounting from compounding and from payment probability.

## Undo accumulation

In this lesson, $t$ denotes a [[payment-time]] without a row index. It is
measured in years from [[valuation-time]] $0$.
Write the [[compounding-frequency]] as $m$, the
[[periodic-rate]] as $r_m$, the [[accumulation-factor]] as
$A(0,t)$, and the [[discount-factor]] as $D(0,t)$.

The accumulation factor $A(0,t)$ is the amount at time $t$ from one unit
invested at time zero. The discount factor is its reciprocal:

$$
D(0,t)=\frac{1}{A(0,t)}=\left(1+r_m\right)^{-mt}.
\label{eq:discount-factor-def}
$$

The discount factor, the periodic rate, and the compounding frequency keep
their different meanings, also when they appear in one expression.

## Meaning before arithmetic

Write the local [[discounted-unit-value]] as $V_0(1_t)$: the value at
valuation time of one deterministic unit paid at $t$. If $D(0,2)=0.94$, the
value at valuation time of one unit paid at $t=2$ years is:

$$
V_0(1_2)
=1\times D(0,2)
=0.94.
$$

The number 0.94 is not an annual interest rate of 94%. It is also not a
probability of 94% that the payment occurs.

<CompactExamples summary="Worked examples (3)">
  <CompactExample label="Annual compounding">

With a [[nominal-annual-rate]] of 5%, $m=1$, and $t=2$, the discount factor
is:

$$
D(0,2)
=\left(1+\frac{0.05}{1}\right)^{-1\times2}
=\frac{1}{1.05^2}
\approx0.907029.
$$

  </CompactExample>
  <CompactExample label="Semiannual compounding">

With a nominal annual rate of 6%, compounded twice per year, the discount
factor for 1.5 years is:

$$
D(0,1.5)
=\left(1+\frac{0.06}{2}\right)^{-2(1.5)}
=\frac{1}{1.03^3}
\approx0.915142.
$$

The horizon of 1.5 years contains three compounding periods. The exponent 1.5
would be wrong, because it counts years, not half-year periods.

  </CompactExample>
  <CompactExample label="Read a small curve">

With the same nominal annual rate of 6%, compounded semiannually, the discount
factors are:

| Time $t$, years | Periods $mt$ | Discount factor $D(0,t)$ |
| --------------: | -----------: | -----------------------: |
|             0.0 |            0 |                 1.000000 |
|             0.5 |            1 |                 0.970874 |
|             1.0 |            2 |                 0.942596 |
|             1.5 |            3 |                 0.915142 |

If the rate is non-negative, the discount factor does not increase with the
payment time. If the rate is positive, the discount factor decreases with the
payment time.

  </CompactExample>
</CompactExamples>

## Check your understanding

These items test meaning, annual calculation, and a semiannual transfer case.
Each question stays collapsed until you open it; answers and explanations appear
once you check.

<AssessmentSet />

## Model boundary and review note

The rate is the same for all times, and the cash flows are deterministic.
This lesson does not teach spot curves, interpolation, day counts, default, recovery,
liquidity, or funding.

The discount-factor definition [[eq-discount-factor-def]] follows
Tuckman & Serrat[@tuckman-serrat-fixed-income; §1.2, the discount factor as the value today of one unit paid at t; §2.4, eqs. 2.17-2.19, the semiannually compounded form]; Hull gives the same interpretation, the present value of one unit paid in the
future[@hull-options-futures; Ch. 4, "Bond Pricing"]. The lesson
stays `draft` pending human confirmation of the printed locators.
