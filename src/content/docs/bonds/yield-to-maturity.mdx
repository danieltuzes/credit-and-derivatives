---
title: Yield to maturity as a single-rate summary
description: Interpret the rate that reproduces a toy bond price, then price from a supplied yield.
lessonId: bonds.yield-to-maturity
editorialStatus: draft
riskTier: 2
estimatedMinutes: 30
requires:
  - bonds.price-from-discount-factors.calculate
  - rates.periodic-rate.calculate
teaches:
  - bonds.yield-to-maturity.interpret
  - bonds.price-from-yield.calculate
assessments:
  - bond-yield-check
sources:
  - tuckman-serrat-fixed-income
  - finra-bond-yield
assumptions:
  - Level fixed coupons and principal redeemed at par
  - Settlement on a coupon date
  - One nominal annual yield compounded at the bond payment frequency
  - The same yield discounts every promised payment
  - No default, liquidity, tax, funding, or embedded option
notation:
  uses:
    - bond-price
    - yield-to-maturity
    - bond-cash-flow
    - bond-payment-frequency
    - coupon-payment
    - face-value
    - maturity-time
    - bond-payment-index
    - number-of-bond-payments
aiAssisted: true
sidebar:
  order: 3
---

## What you will be able to do

After this lesson, you should be able to:

- interpret yield to maturity as a model-dependent single-rate summary;
- state its annualization and compounding convention;
- calculate price from a supplied yield without changing promised cash flows.

## What yield means in this toy model

Use the \term\{bond-payment-index\} $k$ to select one of the
\term\{number-of-bond-payments\} $n$, and write its promised
\term\{bond-cash-flow\} as $CF_k^{\mathrm{bond}}$. Write the
\term\{bond-price\} as $P_0$ and the \term\{bond-payment-frequency\} as
$m_{\mathrm B}$.

Write \term\{yield-to-maturity\} as $y^{(m_{\mathrm B})}$. It is the one
nominal annual rate, compounded at the bond payment frequency, that makes the
discounted promised payments equal the bond price:

$$
P_0
=\sum_{k=1}^{n}
\frac{CF_k^{\mathrm{bond}}}
{\left(1+y^{(m_{\mathrm B})}/m_{\mathrm B}\right)^k}.
$$

The semantic key matters because the glyph $y^{(m_{\mathrm B})}$ could
otherwise be mistaken for a spot rate, effective annual rate, expected return,
or another quote. Here it means only the explicitly defined bond yield.

## Coupon rate and yield do different jobs

- The coupon rate fixes the promised \term\{coupon-payment\} $C$ in the
  contract.
- The supplied yield controls the denominators used by this pricing summary.

Changing the yield input does not rewrite the bond's coupons or
\term\{face-value\} $F$.

## Example 1: annual payments

A two-year, 5% annual-coupon bond with USD 1,000 face value pays USD 50 and USD
1,050. At a 6% annual yield with annual compounding,

$$
P_0
=\frac{50}{1.06}
+\frac{1050}{1.06^2}
\approx\text{USD }981.67.
$$

This migrates the original skeleton's numerical example without changing its
expected value.

## Example 2: semiannual payments and compounding

For the same face value and coupon rate over two years, but with semiannual
payments, $C=25$, $m_{\mathrm B}=2$, and $n=4$. A 6% nominal annual yield gives a
3% periodic rate:

$$
P_0
=\frac{25}{1.03}
+\frac{25}{1.03^2}
+\frac{25}{1.03^3}
+\frac{1025}{1.03^4}
\approx\text{USD }981.41.
$$

The annual and semiannual examples use the same percentage number but not the
same timing or compounding model.

## Example 3: zero-coupon special case

With no coupons and \term\{maturity-time\} $T$,

$$
P_0=\frac{F}{(1+y^{(m_{\mathrm B})}/m_{\mathrm B})^{m_{\mathrm B}T}}.
$$

For USD 1,000 due in three years at 4% with annual compounding, the price is
approximately USD 889.00.

## Check your understanding

The assessment set **bond-yield-check** separates yield interpretation from
annual and periodic-compounding calculations.

## Model boundary and review note

This page prices from a supplied yield; it does not solve numerically for yield
from a price. A real bond implementation also needs schedules, day counts,
accrued interest, clean/dirty price, and potentially credit and option models.

**NEEDS_SOURCE:** exact source locators for yield quotation and interpretation
must be verified before review.
